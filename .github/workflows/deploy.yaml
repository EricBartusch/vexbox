name: Build and Upload VexBox to itch.io 

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - fixthesus
      - testing/ver

jobs:
  call-build-and-upload:
    uses: EricBartusch/godot-container-builder/.github/workflows/reusable-godot-deploy.yaml@feat/publish-release
    with:
      game_name: VexBox
      version: v1.0.1
    secrets:
      USER: ${{ secrets.USER }}
      API_KEY: ${{ secrets.API_KEY }}

